<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wcy.rhapsody.admin.mapper.ColumnMapper">
    <select id="selectPageVo" resultType="com.wcy.rhapsody.admin.model.vo.ColumnVO">
        SELECT s.*,
               COUNT(t.id) AS topics
        FROM section s
                 LEFT JOIN
             topic t
             ON s.id = t.section_id
    </select>
</mapper>